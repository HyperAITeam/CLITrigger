# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

CLITrigger is a full-stack app that automates AI-powered task execution. Users write TODO items in a web UI, and the system spawns isolated git worktrees for each task, running Claude/Gemini/Codex CLI tools in parallel. Built with Express + React + SQLite + WebSocket.

## Commands

```bash
# CLI (npm global install)
npm i -g clitrigger            # global install
clitrigger                     # start server (first run prompts for password)
clitrigger config              # view/change settings (port, password)

# Development (runs server + client concurrently)
npm run dev

# Build
npm run build                  # both client and server (copies client to dist/client/)
npm run build:server           # server only (outputs to dist/server/)
npm run build:client           # client only (outputs to src/client/dist/)

# Production
npm run start                  # serves built app on PORT (default 3000)
npm run start:tunnel           # with Cloudflare Tunnel enabled

# Tests
npm run test                   # all tests (server + client)
npm run test:server            # server tests only (vitest, node env)
npm run test:client            # client tests only (vitest, jsdom env)
npx vitest run src/server/path/to/file.test.ts   # single server test
cd src/client && npx vitest run src/path/to/file.test.tsx  # single client test

# Type checking
npm run typecheck              # server + client
```

## Architecture

### Monorepo Layout

- **`bin/`** — CLI entry point (`clitrigger.js`). Handles first-run setup (password prompt), reads `~/.clitrigger/config.json`, sets env vars, and imports the compiled server. Supports `config` subcommand for port/password/clear changes. Fire-and-forget update check on every launch (npm registry, 5s timeout, silent on failure) prints a one-line `Update available: <v> -> npm i -g clitrigger@latest` hint — does not auto-reinstall or restart. `postuninstall.js` prints cleanup notice on `npm uninstall`.
- **`src/server/`** — Express backend (TypeScript, ESM). Compiled via `tsconfig.server.json` → `dist/server/`.
- **`src/client/`** — React frontend (Vite + TailwindCSS). Has its own `package.json` with separate `npm install`. Dev server proxies API calls to `:3000`. Build output copied to `dist/client/` for npm packaging.
- **`plugin/`** — Hecaton TUI plugin (CommonJS, Deno-compatible). Connects to CLITrigger server as a sidecar client. Built/packaged via `scripts/build-plugin.bat`.

### Server

- **Entry**: `src/server/index.ts` — Express app, middleware, route mounting, graceful shutdown. Auto-retries next port on EADDRINUSE (up to 10 attempts).
- **Database**: `src/server/db/` — SQLite via `better-sqlite3` with WAL mode. Schema uses backward-compatible migrations (adds columns dynamically, never drops tables). 22 tables: `projects`, `todos`, `task_logs`, `schedules`, `schedule_runs`, `cli_models`, `cli_versions`, `plugin_configs`, `discussion_agents`, `discussions`, `discussion_messages`, `discussion_logs`, `sessions`, `session_logs`, `session_raw_chunks`, `planner_items`, `planner_tags`, `memory_nodes`, `memory_edges`, `memory_logs`, `favorites`, `app_settings`. Denormalized `todos.total_cost_usd`/`total_tokens` columns enable SQL-only analytics aggregation. `memory_nodes`/`memory_edges` form a per-project knowledge graph with FK CASCADE on `project_id` and edge endpoints; edges carry a `relation_type` (`related`/`precedes`/`example_of`/`counter_example`/`refines`) with UNIQUE(from,to,relation). `memory_logs` records ingest/lint/retrieve/merge events (event_type, severity, source_type/id/title, message, metadata) for the Wiki Activity sub-tab. `session_raw_chunks` stores binary PTY output per session for xterm.js replay with ~2MB rolling cap per session (FK CASCADE). `sessions` carries `memory_inject_mode`/`memory_node_ids` columns (parity with todos/discussions) so wiki injection runs at session start. `favorites` is a global table (id, name, type, target, args, cwd, icon, sort_order) for user-defined external tool shortcuts (type CHECK: 'executable'|'command'|'url'). `app_settings(key, value, updated_at)` is a global KV table for instance-wide settings (currently `tunnel.name` and `tunnel.hostname`). Visual schema at `docs/ERD.md` (auto-generated from `schema.ts` — regenerate manually via `npm run docs:erd` when schema changes).
- **Routes**: `src/server/routes/` — REST endpoints under `/api/`. Auth, projects (including native folder picker via `POST /browse`, `POST /open-folder`, worktree listing/cleanup; `GET /:id` lazily re-runs `isGitRepository(path)` when `is_git_repo` is falsy and persists the result so post-`git init` projects are recognized on next view), todos, execution (including `POST /continue` for multi-round), logs (with shared `resolveLocalBaseBranch` fallback for `/todos/:id/diff` and `/todos/:id/result`), images, schedules (including `POST /todos/:id/schedule-on-reset` for rate-limit recovery), plugins, models, cli-status (installation check), analytics (per-project cost/execution stats), sessions (interactive-mode entity), planner (lightweight task management, plus Markdown `GET /export` / `POST /import` with `text/markdown` body parser for cross-install transfer), review (read-only `GET /api/review/queue`, `/summary`, plus `/diff/:todoId` and `/diff/:todoId/file` for inline diff expansion with worktree → branch ref → master/main fallback chain and structured reason codes `no-branch`/`branch-missing`/`base-branch-missing`; risk classifier server-side; merge/discard/continue actions reuse existing todo endpoints), memory (`GET /api/projects/:id/memory/{graph,nodes,edges}` CRUD + `POST .../preview` for prompt-block preview, `POST /merge` for atomic node merge with edge redirect + wikilink rewrite + tag union, `POST /assets` + `GET /assets/:filename` for body-image upload, `POST /export` for one-way DB→`.clitrigger/wiki/` Markdown dump, `GET /disk-diff` for external-edit detection, `GET /logs` for the Activity sub-tab, `DELETE /raw-files` for raw source cleanup), tunnel (with `GET/PUT /api/tunnel/config` for custom hostname routing — DB-stored in `app_settings`, hostname pattern validation), discussions (with `POST /:id/extract-planner-items` and `/convert-to-planner` for LLM action-item extraction; merge/diff endpoints share the same base-branch fallback as todos), debug-logs. Integration routes (Jira, GitHub, Notion, gstack, harness) are mounted via the plugin system.
- **Plugins**: `src/server/plugins/` — Modular integration system. Each plugin (jira, github, notion, gstack, harness) is a self-contained module with its own `PluginManifest`, router, and config. Registered in `index.ts` via `registerPlugin()` and auto-mounted via `mountPluginRoutes()`. Two categories: `external-service` (REST proxy + UI panel) and `execution-hook` (orchestrator pre-execution hook). Config stored in generic `plugin_configs` table (key-value per project+plugin). Legacy `projects` table columns maintained for backward compatibility. The `harness` plugin (`src/server/plugins/harness/`) edits Claude/Gemini/Codex user-config files (`~/.claude/`, `~/.gemini/`, `~/.codex/`) — settings, memory, and MCP servers — via per-CLI adapters with atomic writes (temp + rename), deep-merge to preserve untouched fields, and a path-traversal guard. The Codex adapter parses `~/.codex/config.toml` via `@iarna/toml` and surfaces a `trustLevelMissing` warning when the project isn't in the trusted list. Edits are NOT worktree-scoped (they affect the user's global CLI config), so the client renders an explicit warning banner.
- **Services**: `src/server/services/` — Core business logic:
  - `orchestrator.ts` — Task execution engine. Manages concurrency limits, dependency chains, worktree setup with per-project `use_worktree` toggle plus per-todo tri-state override (`todos.use_worktree`: null=inherit, 0=force-main, 1=force-worktree) resolved via `resolveUseWorktree(project, todo)`. Main-branch todos (effective `use_worktree=false`) are gated in `canStartNow` to run alone — if another todo is active the start is deferred and retried from `startDependentChildren` when a sibling finishes. Also handles CLI invocation, auto-chaining of dependent children, squash merge on dependency completion, dependency chain batch merge (leaf branch → main), CLI fallback on context exhaustion AND on Gemini quota exhaustion (sliding-window detector in `log-streamer.ts`), plugin execution hooks (e.g. gstack skill injection), memory injection via `memory-inject-hook.ts` that prepends a `<long_term_memory>` block to the prompt based on per-todo `memory_inject_mode`/`memory_node_ids`, sandbox mode (strict: directory-scoped permissions even for non-worktree runs, permissive: full access), stale process liveness checker (30s interval), multi-round continue (`continueTodo()` reuses existing worktree with `--continue` flag), denormalized cost/token persistence on both success and failure paths for SQL-only analytics, and best-effort `captureReviewMetadata(todoId)` invocation on both success and failure paths to populate review-queue summary/diff stats.
  - `memory-injector.ts` — Builds the `<long_term_memory>` prompt block from a per-project knowledge graph (`memory_nodes` + `memory_edges`). Serializes node body and adjacent edges using arrow-notation (e.g. `Node A —(precedes)→ Node B`). CLI-agnostic — output is plain text prepended to the prompt, so Claude/Gemini/Codex all see identical context with no adapter changes. Four injection modes: `none`, `all`, `selected` (specific node IDs), `auto` (LLM-picked candidates per call). Token-safety: `buildNodeSummary` caps lines per node and falls back to title-only listings when the budget is exceeded; System nodes are excluded from `all`.
  - `memory-retriever.ts` — Backs the `auto` injection mode. Extracts candidates from a project's wiki (title/body/tags) and runs a one-shot headless LLM call to score and select the relevant subset given a query (todo/discussion text, or session title+description). Cost is one extra retrieval call but the main prompt stays compact.
  - `wiki-exporter.ts` — One-way DB → `.clitrigger/wiki/<entity>/<slug>.md` Markdown export with YAML frontmatter (id, tags, edges, source_path). Backs the Wiki tab's Export button and Disk diff/Rebuild flow. Truth source stays in the DB; disk content can live in git or Obsidian but is overwritten by Rebuild.
  - `wiki-index.ts` — Auto-maintains "System" nodes (Schema + Index) shown in a separate sidebar section with an "edits are overwritten" banner. System nodes are excluded from entry groups, retrieval candidates, lint, and `all`-mode injection.
  - `memory-ingest.ts` — Source → wiki ingest pipeline. Splits long sources (>8000 chars) at paragraph boundaries into sequential chunks (max 4 chunks of 7000) and re-fetches nodes between chunks so dedup sees earlier-chunk additions. Tees stdin/stdout/stderr to `debugLogger` (synthetic `mem-{kind}-{sourceType}-{sourceId|ts}` todoId) when `project.debug_logging` is on. Returns a skip breakdown (parse fail, dup, unique, bad id/edge) for the result modal. Auto-ingest results broadcast via `memory:ingest-finished` WS event for the sidebar toast.
  - `memory-inject-hook.ts` — Wraps `memory-injector.ts` for orchestrator/session-manager use. Logs the injection mode and node count to `task_logs`/`discussion_logs`/`session_logs` for visibility. Dispatches to `memory-retriever.ts` for `auto` mode.
  - `discussion-extractor.ts` — Headless one-shot LLM call that converts a completed discussion's transcript into curated planner items. Uses `runHeadless(cliTool, prompt)` with stdin-piped prompt (bypassing Windows cmd.exe argument-length limit), respects `project.cli_tool` (claude/gemini/codex), wraps via `cmd.exe /c` on Windows, and applies a 120s timeout. Parses a tolerant JSON array of `{title, description, priority}` from the model output (ignores code-fence wrapping and surrounding prose). Powers `POST /api/discussions/:id/extract-planner-items` (preview) and `POST /api/discussions/:id/convert-to-planner` (persist).
  - `review-capture.ts` — Best-effort capture of review-queue metadata for finished todos. `pickSummaryFromLogs(todoId)` selects the latest assistant message (or non-empty plain output as fallback) from the most recent round and trims to 240 chars. `computeDiffStats(worktreePath, defaultBranch)` parses `git diff <branch>...HEAD --shortstat` for files/lines. Persists to `todos.summary`/`diff_files`/`diff_lines`. All failures are swallowed so review capture never blocks orchestration.
  - `claude-manager.ts` — Spawns/manages child processes (node-pty for any interactive mode and TTY-requiring tools like Codex, child_process for headless/verbose). Windows cmd.exe wrapper for .cmd shims. Interactive mode uses PTY with stdin wrapped as Writable for WebSocket relay. Uses each adapter's `stdinSubmitSequence` (`\r` for Claude/Codex, `\r\n` for Gemini) for every PTY write path (initial prompt delivery, interactive relay, delayStdin fallback). Adapter-driven `autoRespondRules` handle CLI-specific dialogs (Claude workspace trust, Gemini "Trust folder" + update prompts) without CLI-specific hardcoding; rules flagged `blocking` gate the initial prompt until the dialog clears.
  - `cli-adapters.ts` — Adapter pattern abstracting Claude/Gemini/Codex CLI differences (args, stdin format, output format, session resume, interactive mode). Each adapter declares `supportsInteractive`, `stdinSubmitSequence`, `delayStdinUntilReady`, `readyIndicatorPattern`, and `autoRespondRules[]` (blocking/non-blocking). Supports `SandboxMode` (strict/permissive) per CLI tool. Codex's `continueSession` emits `exec resume --last`; Gemini's emits `--resume latest`. In interactive mode Codex skips the `exec` subcommand to launch the top-level TUI.
  - `log-streamer.ts` — Streams stdout/stderr to DB. Two modes: JSON lines (Claude structured output) and plain text (Gemini/Codex). Claude stream-json events are classified into `assistant`/`tool_use`/`tool_result` types (not flattened to `output`) so the client can render conversational views. Parses token usage, commit hashes, and Claude's `rate_limit_event` (broadcast via WebSocket for reset-time recovery). Detects context exhaustion for CLI fallback chain. Also detects Gemini quota exhaustion via a sliding-window counter (`QUOTA_EXHAUSTION_PATTERN` matching `exhausted your capacity` / `quota will reset`); 3 hits within 60s trips a `quotaKillCallback` (wired by orchestrator to `claudeManager.stopClaude`) so the existing exit-code → `getNextFallbackCli` path takes over — needed because Gemini's CLI retries internally without exiting. Plain-text streams (Gemini/Codex) are gated through `pty-output-filter.isPlainTextNoise(line, state)` with a per-todo `noiseFilterMap` so block state survives across stdout/stderr splits, and the map is cleaned up unconditionally in `getTokenUsage`. Supports round-based log tagging via `setRound()` for multi-round continue.
  - `worktree-manager.ts` — Git worktree lifecycle via `simple-git`. Branch name sanitization (Korean → slug, `feature/` prefix, 40 char max, duplicate suffix `-2`/`-3`). `createWorktree(..., autoInstall)` runs `npm install` in the worktree only when the caller opts in via `project.npm_auto_install` (default OFF); Todo/Discussion/Session orchestrators all pass the same flag through, so the worktree action stays ecosystem-neutral for non-npm projects. Auto-adds `.worktrees` to project `.gitignore`. Squash merge auto-aborts on conflict to prevent dirty worktree state. Also provides 16 Git action methods (stage, unstage, commit, pull, push, fetch, branch, checkout, merge, stash, discard, tag, diff) and commit inspection methods (`getCommitFiles`, `getCommitDiff` with root/merge commit handling) for the web Git client. All `simpleGit(...)` call sites go through the `createGit()` factory in `src/server/lib/git.ts`, which sets `core.quotePath=false` per invocation so non-ASCII filenames (Korean, CJK, emoji) are not C-escaped in diff/status output. The same module exports `resolveLocalBaseBranch(git, configured)` — a shared helper that returns the configured `default_branch` if it exists, falls back to `master`/`main`, otherwise null; consumed by `routes/review.ts`, `routes/logs.ts`, and `routes/discussions.ts` so all diff/merge endpoints share consistent base-branch resolution.
  - `scheduler.ts` — Cron (recurring) and one-time schedules via `node-cron`.
  - `skill-injector.ts` — Injects gstack skill files into `.claude/skills/` in worktrees (Claude CLI only). Used by gstack plugin's `onBeforeExecution` hook.
  - `discussion-orchestrator.ts` — Multi-agent discussion engine. Round-based turn execution where agents speak sequentially, each receiving the full discussion history in their prompt. Supports start/stop/pause/resume, user message injection, turn skipping, auto-implement (automatically triggers implementation round on discussion completion), a special implementation round (max_rounds+1) where a designated agent writes code, and a per-agent `can_implement` flag that lets flagged agents commit on their regular turn (prompt is softened to "prototype the smallest slice" and `project.default_max_turns` is granted in full, not the 10-turn discussion cap). Commits chain naturally on the shared worktree/branch, so the final implementation round keeps its "finish what's missing, commit everything" role. Uses worktree isolation and sandbox mode like todos. Memory injection (`memory_inject_mode`/`memory_node_ids`) prepends the same `<long_term_memory>` block as the todo orchestrator before each agent turn.
  - `session-manager.ts` — Interactive session engine (sibling of `orchestrator`/`discussion-orchestrator`). Wraps `claudeManager` + `logStreamer` to run a single long-lived interactive CLI session per `sessions` row. Supports worktree isolation (opt-in per session), stale-session recovery on startup, `session_logs` retention cleanup, and raw binary PTY output streaming to `session_raw_chunks` table (batched 4KB-or-100ms DB flush, ~2MB rolling cap per session) for xterm.js pixel-perfect playback. Client subscribes to raw bytes via WebSocket; **DB chunks are the single source of truth** — `session:subscribe` calls `flushPendingRaw(sessionId)` (public variant of the in-flight buffer drain) before sending only the persisted chunks (no ring-tail replay), so the welcome banner doesn't get rendered twice. Accepts optional `ptyCols`/`ptyRows` at session start to spawn PTY at actual viewport dimensions. Wiki injection runs via `applyMemoryInjection()` (same as orchestrators) using `${title}\n${description}` as the auto-mode retrieval query. Initial prompts are **stashed in an in-memory `pendingInitialPrompts` map** rather than auto-written to the PTY; clients fetch via `GET /sessions/:id/pending-prompt`, then `POST /submit-initial` (write to PTY) or `/skip-initial` (discard). Pending entries are cleared on stop/exit/error so a stuck panel can't outlive its session. Backs both the Sessions tab (with floating window UI) and its `/cleanup` endpoint.
  - `pty-output-filter.ts` — Noise filter for PTY and headless plain-text streams. Line-level patterns drop CLI spinner frames (including concatenated/bare-\r redraws), status bars, thinking animations, TUI menu indicators (☰ ○ ⏵), worktree path echo, and numbered prompt echo (`3> ...`); cursor sequences are replaced with spaces so word boundaries survive. A stateful block filter (`PtyFilterState.activeBlock` + `blockLineCount`) absorbs multi-line noise: xterm.js parser-state dumps (until outer `}` at column 0) and conpty/node-pty stack traces (until `Node.js vX.X.X`), with a 200-line runaway guard. `isNoiseLine` drops AttachConsole/conpty/xterm.js lines BEFORE the keep-signals `/^Error/` exception so they cannot leak. The `isPlainTextNoise(line, state)` export is consumed by `log-streamer.ts` so the same cleanup applies to headless Gemini/Codex output, not just interactive PTY.
  - `cli-status.ts` — CLI tool installation checker. Runs `--version` on Claude/Gemini/Codex in parallel with 60s caching. Backs `GET /api/cli/status` and surfaces install guidance in the project settings panel. Fire-and-forget triggers `model-sync.ts` on each status check.
  - `model-sync.ts` — CLI model list synchronizer. Probes each CLI's `--help` for supported models and merges with a fallback registry at `src/server/data/cli-models-registry.json` (probe-only loses coverage when CLI help format changes; registry-only misses new models). Flags deprecated entries in `cli_models` and writes the CLI version to `cli_versions`.
  - `debug-logger.ts` — CLI debug logging service. When `project.debug_logging` is enabled, captures raw stdin/stdout/stderr to `.debug-logs/` via PassThrough stream tee (non-invasive to existing log stream). Auto-cleans old logs on startup based on `LOG_RETENTION_DAYS`. Auto-adds `.debug-logs` to project `.gitignore`.
  - `prompt-guard.ts` — Prompt injection detection and sanitization for external inputs (Notion/GitHub/Jira imports).
  - `tunnel-manager.ts` — Cloudflare Tunnel management via `cloudflared` subprocess. Uses bundled npm `cloudflared` package binary as primary source, falls back to system PATH and Windows-specific paths.
- **WebSocket**: `src/server/websocket/` — Real-time log streaming, binary PTY frames, and status broadcasts. Session-authenticated. Supports stdin relay for interactive mode and per-client binary subscription management (session:subscribe/unsubscribe for xterm.js replay, session:terminal-input/session:resize for keystroke/resize relay).
- **Auth**: Session-based (`express-session`), password from `AUTH_PASSWORD` env var (required). Skips `/api/auth/*` and `/api/health`. Server refuses to start without `AUTH_PASSWORD` unless `DISABLE_AUTH=true`. Also disabled when `DISABLE_AUTH=true` (plugin/headless mode).

### Client

- **Entry**: `src/client/src/main.tsx` → `App.tsx` (React Router). Wraps app in `ThemeContext.Provider`, `NotificationProvider`, and `I18nProvider`. Calls `initPlugins()` to register client-side plugins before rendering.
- **Routes**: `/` (ProjectList), `/review` (cross-project Morning Review Queue), `/projects/:id` (ProjectDetail — hosts Todos, Sessions, Planner, Analytics, Wiki, Discussions, Schedules, Git, and plugin tabs including Harness), `/projects/:id/discussions/:discussionId` (DiscussionDetail).
- **API layer**: `src/client/src/api/` — Fetch wrapper with 401 → auto-logout handling. Plugin config API in `plugins.ts`.
- **Plugins**: `src/client/src/plugins/` — Client-side plugin system. Each plugin (jira, github, notion, gstack, harness) provides a `ClientPluginManifest` with `PanelComponent` (tab content), `SettingsComponent` (project settings), `isEnabled()`, and i18n translations. Registered via `registerClientPlugin()` in `plugins/init.ts`. `ProjectDetail.tsx` renders plugin tabs dynamically via `getPluginsWithTabs()`. `ProjectHeader.tsx` renders plugin settings via `getClientPlugins()` loop. The `harness` plugin (`src/client/src/plugins/harness/`) provides Claude/Gemini/Codex tabbed editors for settings, memory files, and MCP servers (CRUD with masked secrets) plus a worktree-isolation warning banner since edits land in the user's global CLI config dirs, not in `.worktrees/<branch>/`.
- **Hooks**: `useAuth` (session state), `useWebSocket` (auto-reconnect with exponential backoff), `useTheme` (light/dark mode via CSS variables + `data-theme` attribute, persisted to localStorage, OS default detection), `useModels` (CLI model list for tool/model selection), `useNotification` (browser Notification API with localStorage-backed on/off + permission flow; dispatched from ProjectDetail/DiscussionDetail on completed/failed events), `useToast` (dispatcher for the shared `Toast` component, 4 variants with progress bar).
- **i18n**: `src/client/src/i18n.tsx` — Context-based Korean/English translations. All UI strings go through `t(key)`. Plugin-specific translations provided by each plugin manifest.
- **Components**: ~60 components in `src/client/src/components/`. Task graph uses `@xyflow/react` + `dagre` for dependency visualization. `GitStatusPanel.tsx` provides a full Git client with a Fork/SourceTree-style layout: left sidebar `WorkspaceMenu` switches between **File Status** view (`WorkingChangesView` — vertical split of staged/unstaged file lists, `WorkingDiffViewer` on the right with binary/empty fallback, commit-message textarea with "push to origin/<branch>" toggle and Cmd/Ctrl+Enter shortcut) and **History** view (commit graph + action toolbar + worktrees section + VS Code-style branch context menu with checkout/merge/rebase/fetch/pull/push/rename/delete + commit detail panel). All four split regions (sidebar↔main, history↔detail, detail file-list↔diff, working file-pane↔diff) use a shared `Resizer` component (axis x/y) with min/max clamping and per-key localStorage persistence. View selection persists per-project via `git-workspace:{projectId}`. `DiffViewer.tsx` is a shared file list + diff viewer (extracted from `GitStatusPanel`) reused by commit-history and review-queue inline expansion; uses fixed dark palette (`bg-[#1A1A1A]`, `text-gray-100/200`, `border-gray-700`) so green/red overlay lines stay readable in both themes. `ReviewQueue.tsx` / `ReviewCard.tsx` render the cross-project `/review` page — card stack with project label, last-assistant summary, token totals, diff stats, server-classified risk badge; `j`/`k`/arrow nav, `Enter` opens an embedded `LogViewer` slide-in detail panel, `Space`/`→` toggles inline `DiffViewer` expansion (with reason-coded fallback messages and a debug-info panel when diff is unavailable), `m` merges via `mergeTodo`, `d` discards via `cleanupTodo`, `Esc` closes/collapses; time-window selector (12h/24h/7d), filter chips (All/Risky/Quick wins/Failed), sticky token ribbon with CLI breakdown (K/M units). Project/branch metadata uses natural casing (no `uppercase` transform) so English branch names like `feature/2-md` and CJK project names display correctly. `Sidebar.tsx` shows the workspace list with inline `+` create button (opens `ProjectForm` modal), hover-revealed `X` delete button per row, a Review Queue link with a 24h pending-count badge, and a **Favorites** section for frequently-used external tools (executables/shell commands/URLs) with Edit/Delete buttons per favorite; dispatches `projects:changed` to keep `ProjectList` in sync. `ProjectList.tsx` shows project cards with invalid-path detection — projects whose local folder no longer exists show a red "경로 없음" badge with dimmed opacity; clicking prompts deletion instead of navigating. `DiscussionDetail.tsx` provides a chat UI for multi-agent discussions (round-grouped messages, streaming logs, user injection, implementation modal, message collapse/expand with summary preview, failure error log panel). On completed discussions it surfaces a **"Send to Planner"** action that opens an extract modal — Loader2 spinner + "extraction can take up to 2 minutes" hint, backdrop/Esc dismissal blocked while `extractLoading` is true, per-item checkbox + inline title/description edit + priority selector before persisting via `convertToPlannerItems`. `MemoryList.tsx` / `MemoryGraph.tsx` / `MemoryNodeDetail.tsx` / `MemoryForm.tsx` host the Wiki tab (UI label "Wiki" / "위키"; backing DB tables stay `memory_nodes`/`memory_edges`) — List view (entry cards with search/tag filter) and Graph view (`@xyflow/react` + dagre auto-layout, drag-to-connect for edges, inline edge-type editing, cycle guard for `precedes`/`refines`), with an entry detail panel showing Markdown body and adjacent edges. `MemoryInjectControl.tsx` is the reusable selector (None/All/Selected modes, entry chips, `<long_term_memory>` prompt-block preview modal) wired into `TodoForm` and `DiscussionForm`. `PlannerItem.tsx` shows a "From Discussion" badge linking back to the source discussion when `source_discussion_id` is set. `TodoList.tsx` supports status filter tabs (All/Active/Completed/Cancelled with counts, localStorage-persisted — flattens hierarchy when not 'all'), iOS-style stack mode (Layers toggle; absolute positioning, 6px peek between cards, front card on top, click to expand, localStorage-persisted), and drag-and-drop reordering via always-rendered gap drop zones between items (16px height + ±8px negative margins overlap the `space-y-3` gap so the layout never shifts during drag; accent line shows only while dragging). `TodoForm.tsx` exposes a tri-state worktree radio (inherit / force-worktree / force-main) for git-repo projects. `TodoItem.tsx` surfaces an inline diff toggle on the changed-files header of the result panel. `SessionWindowsHost.tsx` is a ProjectDetail-scoped context provider that tracks open session **groups** (not single windows) — each group has a layout tree (`stack` | `split` LayoutNodes) so multiple sessions can dock as VS Code-style tabs/splits. State stored under `OpenGroup` (id, geom, z, minimized, root LayoutNode, per-session colors, intent + nonce); persisted to `sessionGroups:{projectId}` localStorage. Public `sessionId`-keyed API (`openOrFocus`, `close`, `focus`, `minimize`, `restore`, `isOpen`) preserved for callers; group-level API adds `closeGroup`, `setSplitSizes`, `setActiveTab`, `reorderTab`, `dockGroup`, `beginTabDrag`. `SessionWindow.tsx` renders via createPortal to document.body, hosts the unified group chrome (visible only when root is split), Aero-style edge snap (8px) + neighbor sticky snap (10px) + corner resize + viewport clamp, and minimize→dock-tray flow. The new `src/client/src/components/group/` directory holds the layout primitives: `groupTree.ts` (pure tree ops with auto-collapse/flatten), `colors.ts` (8-color palette), `SessionPane.tsx` (extracted PTY viewport + phase machine `pendingFit`/`starting`/`subscribed`/`replay-only`/`stopping`/`error`, kept mounted across tab switches so xterm state and live PTY output never drop), `StackView.tsx` (tab bar + pane area, all tabs mount with `display` toggled, A−/A+ font buttons, `data-group-id`/`data-stack-path` for hit-testing), `Splitter.tsx` (4px draggable bar mutating flex-basis directly during drag, commits sizes on mouseup), `LayoutNodeView.tsx` (recursive stack/split dispatcher), `DockOverlay.tsx` (portal-rendered 5-zone diamond + drop preview rect + `detectDockZone` hit-test). Eager tab tearing fires when a tab moves ≥12px outside its source group rect (Chrome/VS Code model). Single-stack groups dock by dragging the wrapper itself with `pointer-events: none` set on the wrapper for the duration of the gesture (so `document.elementFromPoint` finds destination stacks instead of self-hitting). `SessionTerminal.tsx` wraps xterm.js + FitAddon for pixel-perfect PTY rendering; subscribes to WebSocket binary frames and forwards keystrokes/resizes; per-session font size via `useSessionFontSize` (module-level cache + listener set, localStorage `sessionFontSize:{sessionId}`, 8-28px clamp, Ctrl/Cmd ±/= keyboard shortcuts intercepted by `attachCustomKeyEventHandler`); wrapper has `box-sizing: border-box` + `overflow: hidden` and the inner xterm container blocks `wheel` propagation so wheel events don't escape to outer chrome. iOS Safari mobile IME path runs a client-side dubeolsik composer (because iOS doesn't fire compositionevent on the overlay textarea) — input slots cho/jung/jong, decomposes precomposed syllables back so following jamo can extend, flushes on backspace/non-Hangul/Enter/600ms idle. Mobile (<768px) renders fullscreen with active tab only, no docking widgets. `SessionList.tsx` / `SessionForm.tsx` host the Sessions tab — interactive CLI sessions as first-class entities with per-session worktree toggle, inline edit (Edit2 button, gated while running), wiki injection mode/node IDs in form, cleanup button, and a **Send/Skip pre-flight banner** above the terminal viewport when a non-empty initial prompt is held server-side (Preview lazy-loads via `GET /pending-prompt`). `PlannerList.tsx` / `PlannerItem.tsx` / `PlannerForm.tsx` / `PlannerConvertDialog.tsx` provide a lightweight task planner with inline editing, tag management (color cycling, rename/delete), column sorting, image attachments, convert-to-todo/schedule, and Markdown Export/Import buttons (status-section + GFM checkbox + HTML-comment metadata format; hidden file input accepts `.md`/`.markdown`/`text/markdown`; counter-based drag-and-drop overlay scoped to the planner card area; `ioBusy` guard shared between picker and drop paths; object URL download). `AnalyticsPanel.tsx` renders Recharts-based execution stats (BarChart stacked by CLI, donut PieChart for status distribution, cost/tokens LineChart tabs). `LogViewer.tsx` has dual-mode rendering — Chat mode (markdown assistant blocks + collapsible tool_use rows) and Raw mode (flat terminal view); auto-detects mode based on log types and supports an embedded variant (no border/bg) for terminal-frame hosts. `Modal.tsx`, `EmptyState.tsx`, `Toast.tsx`, `Skeleton.tsx` are shared UI primitives (all portal-based where applicable). `DiscussionForm.tsx` provides shared create/edit form for discussions (auto-implement option, agent selection, turn order UI). `AgentManager.tsx` provides CRUD for agent personas with per-agent CLI tool/model selection and a `can_implement` checkbox (Implementer hammer-icon badge on list rows) that lets the agent commit during regular discussion turns. `MarkdownContent.tsx` wraps `react-markdown` + `remark-gfm` for rendering agent responses and discussion messages. `FavoriteForm.tsx` is a modal for creating/editing global favorites with type toggle (executable/command/url), target/args/cwd inputs, and per-type icon selection. Icons use `lucide-react` throughout (brand logo and custom Git graph SVGs kept as inline SVG). `lib/cn.ts` provides the conditional-class utility used across components.

### Key Patterns

- **CLI Adapter Pattern**: All CLI tool differences are isolated in `cli-adapters.ts`. Adding a new CLI means implementing the `CliAdapter` interface.
- **Integration Plugin Pattern**: External service integrations (Jira, GitHub, Notion) and execution hooks (gstack) are self-contained plugins in `src/server/plugins/` and `src/client/src/plugins/`. Each plugin exports a `PluginManifest` (server) and `ClientPluginManifest` (client). Adding a new integration means: create a plugin directory, implement the manifest, and call `registerPlugin()` — no core code changes needed. Config stored in `plugin_configs` table (generic key-value). Two plugin categories: `external-service` (REST proxy routes + panel tab) and `execution-hook` (pre-execution hook in orchestrator).
- **Worktree Isolation**: Each task gets its own git worktree in `.worktrees/`. Child tasks can inherit parent worktrees. Per-project `use_worktree` toggle (default: on) allows running directly on main branch without worktree overhead; when disabled, server forces `max_concurrent=1` to prevent conflicts. Per-todo tri-state override (`todos.use_worktree`: null=inherit, 0=force-main, 1=force-worktree) lets individual tasks opt in/out; main-branch todos are gated to run alone via `canStartNow` and retried via the dependent-children flow when siblings finish.
- **Graceful Shutdown**: Server handles SIGTERM/SIGINT — kills running CLI processes, stops scheduler, closes tunnel. Also shuts down on stdin EOF (plugin sidecar mode).
- **Headless Mode**: `HEADLESS=true` skips static file serving (API-only mode for plugin/embedded use). `DISABLE_AUTH=true` removes auth middleware (local-only plugin scenarios).
- **DB Migrations**: Schema changes add columns with `ALTER TABLE ... ADD COLUMN` guarded by try/catch, so the app works with both old and new DB files. Plugin configs use a separate `plugin_configs` table with automatic migration from legacy project columns.
- **Sandbox Mode**: Per-project `sandbox_mode` (strict/permissive). Strict mode uses each CLI's native sandboxing to restrict file access to the worktree directory. Claude: auto-generated `.claude/settings.json` with absolute-path patterns (`${workDir}/**`) for Read/Edit/Write — relative paths like `./` are ineffective because Claude resolves paths to absolute internally; Codex: `--full-auto` + `--add-dir .git`; Gemini: prompt-level path restriction.
- **Agent Discussion**: Multiple AI agents with different roles (architect, developer, reviewer, etc.) discuss a feature in rounds before implementation. Each agent speaks sequentially with full history context. After discussion completes, a designated agent implements the consensus, OR users can run `discussion-extractor.ts` to convert the transcript into curated planner items via a one-shot headless LLM call (preview + edit + persist flow, with `source_discussion_id` linking back to the originating discussion). Uses same worktree isolation and CLI adapter patterns as todos.
- **Wiki Injection (Karpathy LLM-Wiki pattern)**: Per-project knowledge graph (`memory_nodes` + `memory_edges`) is curated in the Wiki tab and selectively prepended to todo/discussion/session prompts as a `<long_term_memory>` block (XML tag kept verbatim — LLMs treat the literal phrase as a semantic hint). Four modes: `none` (default), `all`, `selected` (specific entry IDs), `auto` (one-shot headless LLM retrieval per call via `memory-retriever.ts`, picks candidates from title/body/tags). The block is plain text, so it works identically across Claude/Gemini/Codex with no adapter changes — `memory-injector.ts` builds it, `memory-inject-hook.ts` wraps with logging, and the orchestrators (and session-manager) prepend it before the existing plugin hook loop. Edges carry typed relations (`related`/`precedes`/`example_of`/`counter_example`/`refines`); the client cycle-guards `precedes`/`refines` to avoid serialization loops. Token-safety: per-node line cap with title-only fallback; lint chunks large wikis; preview modal shows char/token estimates. Maintenance loop: lint surfaces duplicates/orphans/stale with inline fix actions (merge endpoint redirects edges + cascades wikilink rewrites + unions tags atomically). Activity sub-tab (`memory_logs` table) records ingest/lint/retrieve/merge events. Portability: `wiki-exporter.ts` does one-way DB → `.clitrigger/wiki/<entity>/<slug>.md` Markdown export with YAML frontmatter; Disk diff surfaces external edits; Rebuild overwrites disk from DB. System nodes (Schema + Index, auto-maintained by `wiki-index.ts`) live in a separate sidebar section with an "edits are overwritten" banner and are excluded from entry groups, lint, retrieval candidates, and `all`-mode injection. Note: the UI was renamed from "Memory" / "장기기억" to "Wiki" / "위키", but DB tables, API routes (`/api/projects/:id/memory/*`), TypeScript identifiers, file names (`MemoryList.tsx`, `memory-injector.ts`), and the `<long_term_memory>` prompt tag intentionally retain the original "memory" naming to avoid migration risk and preserve the LLM semantic signal.
- **Floating Window + Group Pattern**: `SessionWindow` renders via `createPortal(..., document.body)` so groups survive tab switches and don't get destroyed when ProjectDetail re-mounts. State is managed by `SessionWindowsHost` as a layout tree (`stack` | `split` nodes) per `OpenGroup`, persisted to `sessionGroups:{projectId}` localStorage and gated on `sessions` having loaded so an empty list during loading doesn't nuke restored state. Drag, resize, and split mutate `style.transform` / `flex-basis` directly during gestures (bypass React rerenders, keep xterm canvas stable) and commit to React state on mouseup. Viewport clamping keeps the titlebar ≥80px visible. Aero edge snap (8px from viewport edges) + neighbor sticky snap (10px) live preview via portal during drag. VS Code-style docking: a 5-zone diamond (top/bottom/left/right/center) hit-tests via `document.elementFromPoint` against `data-group-id`/`data-stack-path`-tagged stacks; center drop merges tabs, side drop creates a split sibling. Eager tab tearing detaches a tab into a floating group when it moves ≥12px from the source rect. Single-stack chrome drag detects docks by setting `pointer-events: none` on the wrapper for the gesture (so the elementFromPoint lookup finds destination stacks instead of self-hitting). All tabs in a group stay mounted (`display` toggled) so xterm state and live PTY output never drop on tab switches. Mobile (<768px) collapses to fullscreen of the active tab only, no docking widgets. Apply this pattern for any persistent multi-pane floating UI.
- **Raw Binary Streaming for xterm.js**: Sessions stream raw PTY bytes (not stripped/processed) to enable pixel-perfect terminal rendering. Server maintains a 256KB per-pid ring buffer in `claudeManager` and a ~2MB rolling-cap `session_raw_chunks` DB table for cross-restart replay. **DB chunks are the single source of truth for replay** — `session:subscribe` calls `flushPendingRaw(sessionId)` (drains the in-flight buffer to DB synchronously) before sending only the persisted chunks, with no ring-tail replay (the ring is a strict subset of DB and replaying both produced duplicate welcome banners). Live stream is delivered via the same subscription thereafter. Binary frame parsing bypasses React state (direct per-sessionId callback Map in useWebSocket) for performance. xterm.js receives raw writes; ANSI colors, cursor control, and TUI styling all work identically to native terminal.
- **Tunnel Custom Hostname**: Cloudflare named tunnels can be routed through a user's own domain to inherit its reputation (avoids the "dangerous site" warnings on `*.trycloudflare.com` / `*.cfargotunnel.com`). Server stores `tunnel.name` and `tunnel.hostname` in the global `app_settings` KV table (DB-first, env fallback `TUNNEL_NAME`/`TUNNEL_HOSTNAME`). `tunnel-manager.ts` keeps cloudflared CLI args unchanged — the custom hostname is purely a display/config layer; the user runs `cloudflared tunnel route dns <name> <hostname>` themselves. Validation: hostname pattern regex, rejects `localhost` / spaces / non-domain strings, requires `tunnelName` when `customHostname` is set. CLI: `clitrigger config tunnel hostname <host>|clear`.
- **Failure Tolerance**: On startup, stale "running" todos are reset to "failed", stale "running" discussions are reset to "paused". Plugin execution hook failures are logged but don't block CLI execution.
- **npm CLI Packaging**: Published as `clitrigger` on npm. `bin/clitrigger.js` handles first-run setup, reads config from `~/.clitrigger/config.json`, sets `PORT`/`AUTH_PASSWORD`/`DB_PATH` env vars, then dynamically imports `dist/server/index.js`. Non-blocking update check on every launch prints an upgrade hint; installation is left to the user. Build copies client output to `dist/client/`; server resolves static files from `../client` (npm install) or `../../src/client/dist` (dev) via fallback. Cloudflared bundled as npm dependency for zero-config tunnel.

## Environment

Config via `.env` (see `.env.example`) or `~/.clitrigger/config.json` (npm global install). Key vars: `AUTH_PASSWORD`, `PORT` (default 3000), `DB_PATH` (database location), `TUNNEL_ENABLED`, `TUNNEL_NAME` (named tunnel), `TUNNEL_HOSTNAME` (custom domain for named tunnel), `LOG_RETENTION_DAYS`, `HEADLESS` (skip frontend serving), `DISABLE_AUTH` (skip auth middleware). DB-stored settings (via `app_settings` KV table) take precedence over env vars for tunnel name/hostname.

## Language

UI and documentation are primarily in Korean. Commit messages use Korean or English. The codebase (variable names, comments in code) is in English.

## UI Guidelines

> 시각 디자인 결정(색상, 타이포그래피, spacing, 컴포넌트 스타일)은 `.claude/docs/design.md`를 기준 레퍼런스로 참조할 것.

### Floating elements must render via portal

Tooltips, dropdowns, popovers, hover help boxes, context menus, and "more" menus MUST be rendered through `createPortal(..., document.body)` with `position: fixed` + viewport clamping (using the anchor element's `getBoundingClientRect`). Do NOT rely on `position: absolute` inside the component tree — header cards, list rows, badge rows, and worktree/todo containers all have `overflow`, `border-radius`, or `transform` ancestors that will clip the floating element.

Past bugs from this exact mistake: project header deprecated-model tooltip was clipped by the header card border; todo-item "more" menu (`⋮`) was clipped by the task row. The portal + fixed + clamp pattern lives in `TodoItem.tsx`'s `MoreMenu` and `ProjectHeader.tsx`'s `DeprecatedModelBadge` — copy that pattern for any new floating UI.

Checklist for every floating element:
- Render into `document.body` via `createPortal`
- Use `position: fixed` with top/left computed from the anchor's `getBoundingClientRect()`
- Clamp within viewport: flip above on vertical overflow, shift left on horizontal overflow, keep ≥8px from edges
- Recompute on `scroll` (capture phase) and `resize`
- Use `z-tooltip` z-index class

## Task Execution Guidelines

When working on tasks in this repository (especially via CLITrigger worktrees), follow these efficiency rules:

### Efficiency
- Use grep/glob to find relevant files FIRST. Do NOT read files one by one to explore the codebase.
- Only read files you intend to modify or that are directly needed to understand the change.
- Do NOT launch Agent/Explore subagents for simple, targeted tasks (e.g., CSS changes, config updates, single-file fixes). Use direct grep → read → edit.
- Make all related edits in a single pass. Do not re-read files you already read.
- Prefer `replace_all: true` for repetitive changes across a file.
- Aim for under 15 tool calls for simple tasks, under 30 for complex ones.

### Completion
- Once the task is complete, commit and stop immediately.
- Do not perform additional refactoring, optimization, or testing beyond what was explicitly requested.
- Do not add comments, docstrings, or type annotations to unchanged code.
- Do not review your own changes unless asked.
